import { Header } from '../components/header/Header';
import { LoginForm } from '../components/loginForm/LoginForm';

export const SignIn = () => {
    return (
        <div>
            <Header />
            <LoginForm />
        </div>
    );
};